global:
  scrape_interval:     15s
  evaluation_interval: 15s

scrape_configs:
{% for job in prometheus.jobs %}
  - job_name: {{ job.name }}
    static_configs:
      - targets:
{% for target in job.targets %}
        - {{ target.host }}:{{ target.port }}
{% endfor %}
{% endfor %}
